# Bug Map

Source map: `audit/PROJECT_MAP.md`

Verification performed:

- Backend lint: `npm run lint:check` passed.
- Backend tests: `npm test -- --runInBand` passed, 19 suites, 247 tests, 8 skipped.
- Frontend build: `cd web/admin-spa && npx vite build --outDir /tmp/mighty-admin-spa-audit-dist --emptyOutDir` passed, but emitted warnings for a duplicate `CreateApiKeyModal` component name, stale Browserslist data, many checker warnings, and chunks over 500 kB.
- Dependency audit: `npm audit --omit=dev` failed in the root package with 34 production vulnerabilities, including 1 critical and 7 high. The same command failed in `web/admin-spa` with 7 production vulnerabilities, including 4 high and `xlsx` advisories with no fix available.

Verification update:

- Initial verification re-ran the dependency audits and reproduced the root/frontend production vulnerability counts listed above. After the BUG-013 fix, root and frontend full/production audits all report zero vulnerabilities, and the Node 24 Docker image builds successfully.
- Frontend production build was re-run with `npm run build -- --outDir /tmp/mighty-admin-spa-verify-dist --emptyOutDir`. It passed, reproduced the duplicate `CreateApiKeyModal` warning, reproduced stale Browserslist data, and reproduced chunks over 500 kB.
- Test coverage search found no relevant tests for the audited route-order, logger redaction, CSP, toast escaping, quota-card concurrency, admin login throttling, encryption-key enforcement, balance-script default, webhook outbound filtering, or admin API-key limit validation paths.
- Subsequent fixes added focused quota-card concurrency and webhook outbound filtering coverage. Latest backend verification: `npm test -- --runInBand` passed with 32 suites, 281 passing tests, and 8 skipped; `npm run lint:check` passed.
- Follow-up verification confirmed BUG-008, BUG-017, BUG-020, and BUG-031 with browser, disposable production-install, and in-memory concurrency harnesses. No source code was changed in that verification pass.
- Final confirmed-fix pass fixed BUG-031, BUG-020, BUG-017, and BUG-008. Latest backend verification: `npm test -- --runInBand` passed with 34 suites, 283 passing tests, and 8 skipped; `npm run lint:check` passed; admin SPA build and Playwright refresh verification passed with pre-existing build warnings only.

The `Verification Matrix` at the end of this file is the current source of truth for final bug status. The original tables above are retained as the audit trail and flow map.

| ID | Severity | Confidence | Status | Area | File/Line | Problem | Why it breaks | How to verify | Suggested fix direction |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| BUG-001 | Critical | Proven | Fixed | Security, frontend runtime | `web/admin-spa/src/utils/tools.js:20-85`; callers include `web/admin-spa/src/router/index.js:219`, `web/admin-spa/src/views/ApiStatsView.vue:633`, `web/admin-spa/src/views/ApiKeysView.vue:4010` | Toast rendering previously concatenated `title` and `message` into `innerHTML` without escaping. Many callers pass API or error messages into `showToast`. | Fixed by constructing toast DOM nodes with `document.createElement`, setting caller-provided `title` with `textContent`, and appending message text with `createTextNode` plus explicit `<br>` elements. | Verification: `rg -n "innerHTML|onclick=" web/admin-spa/src/utils/tools.js` returned no matches; `cd web/admin-spa && npm run build -- --outDir /tmp/mighty-admin-spa-bug001-dist --emptyOutDir` passed with pre-existing warnings only. | Changed files: `web/admin-spa/src/utils/tools.js`, `audit/BUG_MAP.md`. |
| BUG-002 | Critical | Proven | Fixed | Security headers, auth blast radius | `src/app.js:160-170`, `src/app.js:179-183`, `src/app.js:262-322`, `src/middleware/auth.js:1889-1907`, `src/middleware/auth.js:2039-2054`, `tests/securityHeaders.test.js` | The admin SPA was served with Helmet CSP disabled, the custom CSP only applied to `/web` and `/`, and `/admin-next/` had an early file-serving shortcut before the later security middleware. | Fixed by adding an admin-SPA CSP helper, applying it to `/admin-next` in `securityMiddleware`, and applying it explicitly in the early `/admin-next/` file-serving shortcut. | Verification: `npm test -- securityHeaders.test.js --runInBand` passed, proving both middleware and early-response CSP paths; `npm run lint:check` passed. | Changed files: `src/middleware/auth.js`, `src/app.js`, `tests/securityHeaders.test.js`, `audit/BUG_MAP.md`. |
| BUG-003 | Critical | Proven | Fixed | Security, logging, privacy | `src/app.js:218-244`, `src/middleware/auth.js:1765-1834`, `src/utils/logger.js:155-172`, `src/routes/web.js:21-105`, `src/routes/userRoutes.js:51-153`, `tests/requestLoggerRedaction.test.js` | The request logger captured non-GET request bodies and JSON response bodies and wrote them through the structured file logger. Login routes receive passwords and return admin/user session tokens. | Fixed by removing request-body metadata from the start/finish logs and removing the `res.json` wrapper that captured response bodies. | Verification: `npm test -- requestLoggerRedaction.test.js --runInBand` passed, proving login-like request passwords and response tokens are absent from logger metadata; `npm run lint:check` passed. | Changed files: `src/middleware/auth.js`, `tests/requestLoggerRedaction.test.js`, `audit/BUG_MAP.md`. |
| BUG-004 | Critical | Proven | Fixed | Storage, auth, security | `scripts/setup.js:57-66`, `scripts/setup.js:72-75`, `src/app.js:486-502`, `src/routes/web.js:211-230`, `src/utils/adminCredentials.js`, `tests/adminCredentials.test.js` | The admin password was persisted in plaintext in `data/init.json` during setup and rewritten in plaintext on password change. App startup hashed the plaintext file each time. | Fixed by storing `adminPasswordHash` instead of `adminPassword`, migrating existing plaintext init files to hash-only storage on load, and writing only hashes on password change. | Verification: `npm test -- adminCredentials.test.js --runInBand` passed for setup data, plaintext migration, and password update; `npm run lint:check` passed. | Changed files: `src/utils/adminCredentials.js`, `scripts/setup.js`, `src/app.js`, `src/routes/web.js`, `tests/adminCredentials.test.js`, `audit/BUG_MAP.md`. |
| BUG-005 | Critical | Proven | Fixed | Async race, database/storage, quota cards | `src/services/quotaCardService.js:46-65`, `src/services/quotaCardService.js:318-430`, `tests/quotaCardConcurrency.test.js` | Card redemption previously read `status`, mutated API key quota/expiry, and only later marked the card redeemed. | Fixed by wrapping redemption in a per-card Redis lock, re-reading card state inside the lock, and rejecting the second concurrent redemption after the first marks the card redeemed. | Verification: `npx jest tests/quotaCardConcurrency.test.js --runInBand` and full `npm test -- --runInBand` passed; the focused test proves two concurrent same-card redemptions produce one success, one rejection, one quota addition, and one redemption record. | Changed files: `src/services/quotaCardService.js`, `tests/quotaCardConcurrency.test.js`, `audit/BUG_MAP.md`. |
| BUG-006 | High | Proven | Fixed | Async race, state/storage, API key limits | `src/services/apiKeyService.js:2738-2838`, `src/services/apiKeyService.js:2887-2963`, `src/services/quotaCardService.js:376-415`, `tests/quotaCardConcurrency.test.js` | API key quota and expiry updates previously used stale read-modify-write logic. | Fixed by using a Redis Lua mutation for total cost limit increments and a per-key Redis lock for expiry extension calculations. Quota-card limits now pass max caps into those mutation paths and record actual capped additions. | Verification: `npx jest tests/quotaCardConcurrency.test.js --runInBand` and full `npm test -- --runInBand` passed; focused tests prove concurrent `+5` and `+7` quota updates end at `12` and two concurrent one-day expiry extensions end at `2030-01-03T00:00:00.000Z`. | Changed files: `src/services/apiKeyService.js`, `src/services/quotaCardService.js`, `tests/quotaCardConcurrency.test.js`, `audit/BUG_MAP.md`. |
| BUG-007 | High | Proven | Fixed | Backend routing, broken user flow | `src/routes/userRoutes.js:12-19`, `src/routes/userRoutes.js:506-575`, `src/routes/userRoutes.js:835`, `src/routes/userRoutes.js:859`, `tests/userRoutesOrder.test.js` | `GET /users/:userId` was registered before fixed one-segment routes `GET /users/redemption-history` and `GET /users/quota-info`. | Fixed by adding a reserved-route skip guard before the admin `/:userId` middleware chain, so fixed user route names continue to later route handlers before admin auth can reject them. | Verification: `npm test -- userRoutesOrder.test.js --runInBand` passed for both `/users/redemption-history` and `/users/quota-info`; `npm run lint:check` passed. | Changed files: `src/routes/userRoutes.js`, `tests/userRoutesOrder.test.js`, `audit/BUG_MAP.md`. |
| BUG-008 | High | Proven | Fixed | Frontend state management, auth flow | `web/admin-spa/src/stores/auth.js`, `web/admin-spa/src/router/index.js` | Admin auth restored `authToken` from `localStorage`, but the router guard checked `isAuthenticated` before auth initialization. | Fixed by making admin `checkAuth()` await token verification and return a boolean, then making the protected-route guard await it before redirecting to login. | Verification: admin SPA build passed; Playwright loaded `/admin/dashboard` with `localStorage.authToken` and a mocked successful `/webapi/web/auth/user`, and final URL remained `/admin/dashboard`. | Changed files: `web/admin-spa/src/stores/auth.js`, `web/admin-spa/src/router/index.js`. |
| BUG-009 | High | Proven | Fixed | Auth/session lifecycle | `src/routes/web.js:117-123`, `web/admin-spa/src/utils/http_apis.js:26-28`, `web/admin-spa/src/stores/auth.js:53-67`, `web/admin-spa/src/components/layout/AppHeader.vue:480-509` | The backend had `POST /web/auth/logout` that deletes the session, but the admin SPA logout only cleared local state and never called it. | Fixed by adding `logoutApi`, making the auth store call backend logout before local cleanup, and awaiting the store logout action from the header logout flow. | Verification: source search found `logoutApi` calling `/web/auth/logout` and `await authStore.logout()` in the header; `cd web/admin-spa && npm run build -- --outDir /tmp/mighty-admin-spa-bug009-dist --emptyOutDir` passed with pre-existing warnings only. | Changed files: `web/admin-spa/src/utils/http_apis.js`, `web/admin-spa/src/stores/auth.js`, `web/admin-spa/src/components/layout/AppHeader.vue`, `audit/BUG_MAP.md`. |
| BUG-010 | High | Proven | Fixed | API/backend performance, unauthenticated endpoint | `src/app.js:34-42`, `src/app.js:443-457`, `src/models/redis.js:2710-2726` | `/metrics` was unauthenticated and called `redis.getSystemStats()`, which scans `apikey:*`, `claude:account:*`, and `usage:*` keyspaces on every request. | Fixed by adding existing `authenticateAdmin` middleware to the `/metrics` route before Redis system stats collection. | Verification: `rg -n "app\\.get\\('/metrics', authenticateAdmin" src/app.js` confirmed the auth middleware is mounted; `npm run lint:check` passed. | Changed files: `src/app.js`, `audit/BUG_MAP.md`. |
| BUG-011 | Medium | Proven | Fixed | Environment/config, security | `src/app.js:1-195`, `src/middleware/auth.js:1721-1766`, `tests/corsPolicy.test.js` | The example config enables `ENABLE_CORS=true`, which previously made the app use bare `cors()` and allow any origin rather than the custom restricted middleware. | Fixed by removing the bare `cors()` branch so the app always applies the restricted CORS middleware. | Verification: `npm test -- corsPolicy.test.js --runInBand` passed, proving arbitrary origins are not reflected while configured local origins still work; `npm run lint:check` passed; source search found no remaining `app.use(cors())` or `require('cors')` usage. | Changed files: `src/app.js`, `tests/corsPolicy.test.js`, `audit/BUG_MAP.md`. |
| BUG-012 | Low | Proven | Fixed | Validation/input handling | `src/utils/pagination.js`, `src/routes/userRoutes.js:839-855`, `src/routes/apiStats.js:1648-1675`, `tests/userRoutesOrder.test.js`, `tests/apiStatsRedemptionPagination.test.js` | Redemption history `limit` and `offset` were parsed without NaN, integer, lower-bound, or upper-bound validation. | Fixed by adding shared pagination validation and applying it to both user and public redemption-history routes before service/Redis lookups. | Verification: `npm test -- userRoutesOrder.test.js apiStatsRedemptionPagination.test.js --runInBand` passed; `npm run lint:check` passed. | Changed files: `src/utils/pagination.js`, `src/routes/userRoutes.js`, `src/routes/apiStats.js`, `tests/userRoutesOrder.test.js`, `tests/apiStatsRedemptionPagination.test.js`, `audit/BUG_MAP.md`. |
| BUG-013 | Critical | Proven | Fixed | Dependency/version security | `package.json:52-76`, `package.json:91-93`, `package-lock.json`, `pnpm-lock.yaml`, `web/admin-spa/package.json:14-24`, `web/admin-spa/package-lock.json`, `Dockerfile:2`, `Dockerfile:15`, `Dockerfile:34`, `.github/workflows/pr-lint-check.yml:39`, `.github/workflows/auto-release-pipeline.yml:163` | Production dependency audits reported known vulnerabilities. Root audit originally reported 34 vulnerabilities, including critical `fast-xml-parser` via AWS SDK dependencies. Frontend audit originally reported 7 vulnerabilities, including high advisories for `xlsx`. | Fixed by raising the runtime, Docker, CI, installer, and documentation baseline to Node 24, upgrading AWS SDK packages to `^3.1057.0`, applying root and frontend audit fixes, upgrading frontend Vite tooling to a clean line, and refreshing npm/pnpm lockfiles. | Verification: root `npm audit --json` and `npm audit --omit=dev --json` both report zero vulnerabilities; frontend `npm audit --json` and `npm audit --omit=dev --json` report zero vulnerabilities; `corepack pnpm install --lockfile-only` completed; `DOCKER_BUILDKIT=1 docker build -t mighty-audit-node24 .` passed with Node 24 and zero npm audit vulnerabilities during install stages. | Changed files: `Dockerfile`, `package.json`, `package-lock.json`, `pnpm-lock.yaml`, `web/admin-spa/package.json`, `web/admin-spa/package-lock.json`, `README.md`, `README_EN.md`, `scripts/manage.sh`, `.github/workflows/pr-lint-check.yml`, `.github/workflows/auto-release-pipeline.yml`, `audit/BUG_MAP.md`. |
| BUG-014 | Low | Proven | Fixed | Frontend build, component registration | `web/admin-spa/vite.config.js` | The frontend build warned that component `CreateApiKeyModal` had naming conflicts and one auto-registered component was ignored. | Fixed by excluding the already explicitly imported user create-key modal from component auto-registration, removing the ambiguous auto-registration without renaming files. | Verification: `cd web/admin-spa && npm run build -- --outDir /tmp/mighty-admin-spa-bug01415-dist --emptyOutDir` passed and did not emit the duplicate `CreateApiKeyModal` warning. | Changed files: `web/admin-spa/vite.config.js`. |
| BUG-015 | Low | Proven | Fixed | Performance, build output | `web/admin-spa/vite.config.js`, `web/admin-spa/src/main.js`, `web/admin-spa/src/views/ApiKeysView.vue`, `web/admin-spa/package-lock.json` | The production build emitted chunk-size warnings and stale Browserslist/caniuse-lite warnings. | Fixed by updating Browserslist data, removing the full Element Plus plugin install in favor of existing auto-imported components, splitting vendor chunks more granularly, and loading the XLSX export library only on export as a static asset. | Verification: `cd web/admin-spa && npm run build -- --outDir /tmp/mighty-admin-spa-bug01415-dist --emptyOutDir` passed with no Browserslist stale-data warning and no chunks-over-500-kB warning. | Changed files: `web/admin-spa/vite.config.js`, `web/admin-spa/src/main.js`, `web/admin-spa/src/views/ApiKeysView.vue`, `web/admin-spa/package-lock.json`. |
| BUG-016 | Medium | Proven | Fixed | Missing tests for risky logic | `tests/securityHeaders.test.js`, `tests/requestLoggerRedaction.test.js`, `tests/apiKeyQueryAuth.test.js`, `tests/adminCredentials.test.js`, `tests/userRoutesOrder.test.js`, `tests/adminLoginRateLimit.test.js`, `tests/configSecrets.test.js`, `tests/featureFlags.test.js`, `tests/corsPolicy.test.js`, `tests/accountBalanceService.test.js`, `tests/adminApiKeysPayloadRulesRoute.test.js`, `tests/apiStatsRedemptionPagination.test.js`, `tests/toastEscapingStatic.test.js`, `tests/quotaCardConcurrency.test.js`, `tests/webhookOutboundPolicy.test.js` | Existing tests did not cover several confirmed high-risk paths from the audit. | Fixed for confirmed-risk paths by adding focused regression coverage for CSP, logger redaction, query-string API-key rejection, admin credential storage, user route ordering, admin login throttling, production secret enforcement, balance-script default-off behavior, CORS policy, balance error signaling, API-key limit validation, redemption pagination, toast HTML-sink removal, quota-card concurrency, and webhook outbound filtering. | Verification: focused quota/webhook suite `npx jest tests/quotaCardConcurrency.test.js tests/webhookOutboundPolicy.test.js --runInBand` passed with 8 tests; full backend suite `npm test -- --runInBand` passed with 32 suites, 281 passing tests, and 8 skipped; `npm run lint:check` passed. | Changed files: added/updated the listed tests plus `audit/BUG_MAP.md`. |
| BUG-026 | Critical | Proven | Fixed | Security/config, encrypted secret storage | `config/config.js:1-40`, `config/config.example.js:1-40`, `.env.example:9-12`, `src/utils/commonHelper.js:27-43`, `src/services/account/openaiAccountService.js:468-477`, `src/services/account/bedrockAccountService.js:666-688`, `tests/configSecrets.test.js` | Runtime config fell back to public placeholder secrets, including `ENCRYPTION_KEY`, and the example env also used placeholder values. Account services derive encryption keys from this config and encrypt OAuth tokens, API keys, and cloud credentials with it. | Fixed by validating `JWT_SECRET` and `ENCRYPTION_KEY` at config load in production, rejecting missing, too-short, or public placeholder values while preserving development/test fallback behavior. | Verification: `npm test -- configSecrets.test.js --runInBand` passed for production JWT placeholder rejection, encryption-key placeholder rejection, and valid production secrets; `npm run lint:check` passed. | Changed files: tracked `config/config.example.js`, local ignored `config/config.js` mirror in this workspace, `tests/configSecrets.test.js`, `audit/BUG_MAP.md`. |
| BUG-027 | High | Proven | Fixed | Auth/security, admin login brute force | `src/routes/web.js:13-82`, `src/routes/web.js:85-153`, `tests/adminLoginRateLimit.test.js` | Admin login previously had no per-IP or per-username throttling. | Fixed by adding Redis-backed admin login limits keyed by client IP and client-IP plus normalized username hash before credential lookup or password comparison. | Verification: `npm test -- adminLoginRateLimit.test.js --runInBand` passed, proving exceeded login attempts return `429` with `Retry-After` before admin credential lookup; `npm run lint:check` passed. | Changed files: `src/routes/web.js`, `tests/adminLoginRateLimit.test.js`, `audit/BUG_MAP.md`. |
| BUG-028 | High | Proven | Fixed | Account Balance Flow, server-side script execution | `config/config.js:237-242`, `config/config.example.js:237-242`, `src/utils/featureFlags.js:22-41`, `tests/featureFlags.test.js`, `tests/accountBalanceService.test.js` | Balance scripts were effectively enabled by default through config even though the feature flag helper says the safe default is disabled. | Fixed by making the config default require `BALANCE_SCRIPT_ENABLED=true`, so the existing feature flag helper and route checks now fail closed when the env var is absent. | Verification: `npm test -- featureFlags.test.js accountBalanceService.test.js --runInBand` passed, proving default-disabled and explicit-enable behavior; `npm run lint:check` passed. | Changed files: tracked `config/config.example.js`, local ignored `config/config.js` mirror in this workspace, `tests/featureFlags.test.js`, `audit/BUG_MAP.md`. |
| BUG-029 | High | Proven | Fixed | Webhook Flow, SSRF/outbound egress | `src/utils/outboundNetworkPolicy.js`, `src/services/webhookService.js:287-348`, `src/services/webhookService.js:468-497`, `tests/webhookOutboundPolicy.test.js` | Webhook send paths previously validated URL syntax but did not block private, loopback, link-local, metadata, or DNS-rebound hosts. SMTP host and Telegram proxy/base URLs also became server-side outbound connections. | Fixed by adding shared outbound host/URL policy checks, blocking private/special IP ranges after DNS resolution, pinning validated DNS records for HTTP(S) requests, disabling HTTP redirects, validating SMTP hosts, and blocking unsafe Telegram proxies. | Verification: `npx jest tests/webhookOutboundPolicy.test.js --runInBand` and full `npm test -- --runInBand` passed; focused tests prove loopback HTTP URLs, private DNS answers, SMTP loopback hosts, and Telegram loopback proxies are blocked before network send. | Changed files: `src/utils/outboundNetworkPolicy.js`, `src/services/webhookService.js`, `tests/webhookOutboundPolicy.test.js`, `audit/BUG_MAP.md`. |
| BUG-030 | Medium | Proven | Fixed | Validation/input, admin API-key creation | `src/routes/admin/apiKeys.js:48-81`, `src/routes/admin/apiKeys.js:1603-1615`, `src/routes/admin/apiKeys.js:1787-1830`, `tests/adminApiKeysPayloadRulesRoute.test.js` | Admin single-key creation validated `totalCostLimit` but not `rateLimitCost`, `dailyCostLimit`, or `weeklyOpusCostLimit`; batch creation skipped most numeric limit validation. | Fixed by adding shared non-negative finite cost-limit validation for create and batch-create, and by applying the existing integer limit validations to batch-create before calling `generateApiKey`. | Verification: `npm test -- adminApiKeysPayloadRulesRoute.test.js --runInBand` passed for invalid single-create and batch-create limit inputs; `npm run lint:check` passed. | Changed files: `src/routes/admin/apiKeys.js`, `tests/adminApiKeysPayloadRulesRoute.test.js`, `audit/BUG_MAP.md`. |
| BUG-031 | High | Proven | Fixed | Relay Request Flow, async quota/cost race | `src/middleware/auth.js`, `tests/costLimitConcurrencyLock.test.js` | Rate-window, daily, total, and weekly cost limits were checked before relay using stale cost snapshots while actual cost was recorded after response. | Fixed by serializing cost-limit checks per API key, revalidating the key/cost snapshot inside the gate, estimating rated request cost, and rejecting requests whose projected cost would exceed the configured limit. | Verification: `npx jest tests/costLimitConcurrencyLock.test.js --runInBand`; full `npm test -- --runInBand`; `npm run lint:check`. | Changed files: `src/middleware/auth.js`, `tests/costLimitConcurrencyLock.test.js`. |

## User Flow Trace Findings

These rows trace the important flows from `audit/PROJECT_MAP.md` as input -> validation -> business logic -> database/storage -> response/output -> UI/state update. Existing findings are cross-referenced where the flow audit reaches the same defect.

| ID | Severity | Confidence | Status | Flow Name | Flow Trace | Flow Starts | Failure Point | User Experience | Evidence | How to Reproduce/Test | Suggested Fix Direction |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| BUG-017 | High | Proven | Fixed | Bootstrap And Startup | CLI start command -> app bootstrap -> Redis/session setup -> Express routes/static assets -> server listening | `package.json`, `nodemon.json` | Startup ran `npm run lint`, and `lint` runs `eslint ... --fix`, so production installs without dev dependencies failed before app startup and writable checkouts could be mutated during start. | Fixed by making runtime and nodemon startup execute only `node src/app.js`; lint remains available through explicit lint scripts. | Verification: disposable production install with `npm ci --omit=dev --ignore-scripts` ran `npm start` successfully against a dummy `src/app.js`; `npm run lint:check` passed. | Changed files: `package.json`, `nodemon.json`. |
| BUG-018 | High | Probable | Open | Admin Login And Admin SPA | Login form -> credential validation -> Redis session creation -> token response -> SPA stores token -> logout clears UI state -> expected Redis session deletion | Admin header logout button | The SPA logout only clears local auth state and routes to login; it never calls `POST /web/auth/logout`, the endpoint that deletes the backend session. | Logout appears successful, but any copied or stolen bearer token remains valid until normal expiry. | Existing BUG-009. Backend invalidation exists at `src/routes/web.js:117-123`; frontend logout only clears state at `web/admin-spa/src/stores/auth.js:53-58` and `web/admin-spa/src/components/layout/AppHeader.vue:498-510`. | Log in, save the bearer token, click logout, then call `GET /web/auth/user` with the saved token. The session should still be accepted if it was not deleted. | Have the SPA call the backend logout endpoint before clearing local state, while still clearing local state if the request fails. |
| BUG-019 | Critical | Proven | Open | Account Management | Account form/OAuth exchange -> admin auth -> route validation -> account service stores credentials -> JSON response -> account table updates | Admin account create/update APIs from the accounts UI | The global request logger records non-GET request bodies and JSON responses after parsing. Account management requests include OAuth codes, refresh tokens, API keys, proxy credentials, and cloud credentials. | Account creation succeeds normally, but provider secrets can be written to application logs and later exposed through log access, backups, or support bundles. | Existing BUG-003. Account APIs are wired from `web/admin-spa/src/utils/http_apis.js:120-179`; OpenAI create starts at `src/routes/admin/openaiAccounts.js:319-440`; logger records `meta.req` and `meta.res` at `src/middleware/auth.js:1813-1827`. | Create a provider account in a test environment with a known fake secret, then inspect `logs/claude-relay-*.log` for the submitted secret or returned token fields. | Redact or stop logging request and response bodies by default. Allowlist safe metadata only for credential-bearing routes. |
| BUG-020 | High | Proven | Fixed | API Key Creation And Use | User modal input -> client submits `/users/api-keys` -> name and cost validation -> per-user creation lock -> current user key count read -> key created -> user key count written -> UI shows new key | `src/routes/userRoutes.js`, `tests/userApiKeyCreationLock.test.js` | The max-key check and key creation were read-modify-write operations with no lock or transaction. Concurrent creates could both pass the limit check. | Fixed by wrapping the user key-count check, create, and count update in a per-user Redis lock and returning `429` if the lock cannot be acquired promptly. | Verification: `npx jest tests/userApiKeyCreationLock.test.js --runInBand`; full `npm test -- --runInBand`; `npm run lint:check`. | Changed files: `src/routes/userRoutes.js`, `tests/userApiKeyCreationLock.test.js`. |
| BUG-021 | Critical | Proven | Fixed | Relay Request Flow | Client relay request -> API key extraction -> key validation/quota checks -> scheduler/provider relay -> usage counters/logs -> model response | Relay endpoints such as `POST /api/v1/messages` and `/openai/...` | The relay accepted an API key from `req.query.key`; request logging also recorded the original URL and query string. URL-carried secrets leaked through browser history, reverse proxies, access logs, and app logs. | Fixed by removing `req.query.key` from API-key extraction and logging query-bearing URLs as `path?[query]` without raw query metadata. | Evidence: `src/middleware/auth.js:160-179`, `src/middleware/auth.js:1765-1833`, `tests/apiKeyQueryAuth.test.js`, `tests/requestLoggerRedaction.test.js`. | Verification: `npm test -- apiKeyQueryAuth.test.js requestLoggerRedaction.test.js --runInBand` passed; `npm run lint:check` passed. | Changed files: `src/middleware/auth.js`, `tests/apiKeyQueryAuth.test.js`, `tests/requestLoggerRedaction.test.js`, `audit/BUG_MAP.md`. |
| BUG-022 | Critical | Proven | Fixed | Public API Stats Flow | Public stats page input or URL query -> key/id lookup -> stats API requests -> localStorage persistence -> stats UI updates | `/admin-next/api-stats` public page | The public stats store saved the full API key in `localStorage`, and the view accepted an `apiKey` URL parameter. Both were sensitive storage/transport locations for a bearer-style relay credential. | Fixed by saving only `lastApiId`, removing legacy `lastApiKey`, ignoring and stripping `apiKey` URL parameters, and loading remembered stats by non-secret API key ID. | Evidence: `web/admin-spa/src/stores/apistats.js:153-158`, `web/admin-spa/src/stores/apistats.js:415-426`, `web/admin-spa/src/views/ApiStatsView.vue:799-823`. | Verification: `rg` found no `lastApiKey` persistence or `loadApiKeyFromStorage` usage; `cd web/admin-spa && npm run build -- --outDir /tmp/mighty-admin-spa-bug022-dist --emptyOutDir` passed with pre-existing warnings only. | Changed files: `web/admin-spa/src/stores/apistats.js`, `web/admin-spa/src/views/ApiStatsView.vue`, `audit/BUG_MAP.md`. |
| BUG-023 | Medium | Proven | Fixed | User Management Flow | Admin toggles user status -> `/users/:id/status` updates user -> disabled user makes a request -> frontend interceptor clears auth -> login redirect -> UI state resets | Admin user status toggle and disabled user's next API response | The user-store 403 interceptor redirected to absolute `/user-login` instead of using the configured SPA base path. | Fixed by deriving the disabled-user login redirect from `APP_CONFIG.basePath`, so production builds under `/admin-next/` redirect to the mounted SPA route. | Verification: source search confirmed no remaining `window.location.href = '/user-login'` or `pathname !== '/user-login'` in `web/admin-spa/src/stores/user.js`; `cd web/admin-spa && npm run build -- --outDir /tmp/mighty-admin-spa-bug023-dist --emptyOutDir` passed with pre-existing warnings. | Changed files: `web/admin-spa/src/stores/user.js`, `audit/BUG_MAP.md`. |
| BUG-024 | Critical | Probable | Open | Quota Card Flow | Card code and target key -> route validates user/key ownership or public apiId -> redemption service reads card -> mutates key quota/expiry -> marks card redeemed -> writes redemption indexes -> UI shows success/history | `/apiStats/api/redeem-card` and `/users/redeem-card` | Redemption mutates API key quota/expiry before atomically claiming the card, and writes card status and redemption records later. Concurrent requests or mid-flow Redis failures can double-credit or leave partial state. | A card can be redeemed twice under concurrency, or a failed redemption can add quota/time while leaving the card reusable or missing from history. | Existing BUG-005 and BUG-006. Public route starts at `src/routes/apiStats.js:1529-1595`; user route starts at `src/routes/userRoutes.js:771-806`; service checks status then applies side effects at `src/services/quotaCardService.js:293-305`, `src/services/quotaCardService.js:355-393`, and records redemption at `src/services/quotaCardService.js:420-458`. | Fire two concurrent redeem requests for the same unused card, or inject a Redis failure after quota addition but before redemption record writes. Inspect card status, API key quota/expiry, and redemption history. | Atomically claim the card first with Redis Lua, WATCH/MULTI, or a lock. Make side effects idempotent and store redemption outcome in one transaction-like operation. |
| BUG-025 | Medium | Proven | Fixed | Account Balance Flow | Click refresh visible balances -> platform validated -> provider/script queried -> balance response returned -> UI merges balance data and counts success/failure | Accounts page refresh balances action | Provider and script failures were converted into a response with top-level `success: true` and `data.status: error`; the accounts UI treated top-level success as a successful refresh. | Fixed by making account-balance responses set top-level `success` from `data.status`, so error-status provider/script results are failure responses. | Verification: `npm test -- accountBalanceService.test.js --runInBand` passed, including provider failure returning `success: false`; `npm run lint:check` passed. | Changed files: `src/services/account/accountBalanceService.js`, `tests/accountBalanceService.test.js`, `audit/BUG_MAP.md`. |

## Adversarial Review Notes

These notes are about audit quality and coverage, not separate application bugs unless an ID is listed above.

| Topic | Affected IDs | Assessment | Follow-up |
| --- | --- | --- | --- |
| Missing categories added | `BUG-026` through `BUG-031` | The prior audit under-covered secret-management defaults, admin login brute force, server-side script execution, webhook outbound egress, admin API-key validation gaps, and cost-limit concurrency. | Treat the six new rows as unique findings and prioritize reproduction for `BUG-027`, `BUG-028`, `BUG-029`, and `BUG-031`. |
| Duplicate flow rows | `BUG-018`, `BUG-019`, `BUG-024` | These are intentionally duplicated flow-trace rows for existing bugs: `BUG-018` maps `BUG-009`, `BUG-019` maps `BUG-003`, and `BUG-024` maps `BUG-005` plus `BUG-006`. They should not be counted as additional unique defects. | Keep them for flow traceability, but deduplicate during remediation planning. |
| Weak evidence needing live reproduction | `BUG-001`, `BUG-025` | These older notes originally included BUG-008, BUG-017, BUG-020, and BUG-031, but those were later confirmed by targeted browser, production-install, and concurrency harnesses. | Add proof scripts or manual test notes before using remaining weak-evidence rows as release-blocking evidence. |
| False-positive candidates | `BUG-028` | For `BUG-028`, the default-on mismatch is proven, but exploitability beyond outbound script execution depends on deployment and VM escape testing. | Keep current severity unless reproduction changes impact. Mark false positive only after targeted tests. |
| Too broad for remediation tracking | `BUG-013` | Dependency vulnerabilities are aggregated into one row. That is useful for audit visibility but too broad for patch planning. | Split into package/advisory-specific tickets before fixing dependencies. |
| Risky flows still not fully audited | none yet | Data export/import and sync, OAuth polling/callback edge cases, request-detail body-preview privacy, pricing update/check-update outbound calls, admin concurrency cleanup, and scheduled account tests were not deeply traced in this pass. | Add a follow-up read-only audit if those operational flows matter for the release. |

## Verification Matrix

Final verification statuses are based on code inspection, existing tests/builds, dependency audit output, and reproducible manual steps where available. `Confirmed` means the defect or risky behavior is directly proven. `Probable` means code evidence is strong but the exact runtime/concurrency/manual proof was not executed in this phase. `Needs Verification` means the finding remains plausible but user impact is not proven. `Duplicate` means the row repeats another verified bug.

Post-fix probable verification added controlled harness evidence for the highest-risk rows:

- `BUG-005`: in-memory `redeemCard` harness returned two successes, two redemption records, and `totalAdded: 20` for one 10-credit card.
- `BUG-006`: in-memory `addTotalCostLimit` harness ended with `finalTotalCostLimit: 7` for concurrent `+5` and `+7` updates, where atomic behavior should end at `12`.
- `BUG-029`: local `127.0.0.1` listener received a POST from `webhookService.sendHttpRequest`, proving loopback egress through the webhook sender.
- `BUG-008`, `BUG-017`, `BUG-020`, and `BUG-031` were confirmed by targeted browser, production-install, and concurrency harnesses, then fixed and re-verified.

| ID | Final Status | Evidence | Reproduction Steps | Expected Result | Actual Result | Final Severity | Final Confidence | Fix Priority | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| BUG-001 | Fixed | `showToast` now constructs DOM nodes without caller-controlled HTML parsing in `web/admin-spa/src/utils/tools.js:20-85`; caller strings are assigned via `textContent` or `createTextNode`. | Verification command: `rg -n "innerHTML|onclick=" web/admin-spa/src/utils/tools.js`; build command: `cd web/admin-spa && npm run build -- --outDir /tmp/mighty-admin-spa-bug001-dist --emptyOutDir`. | Toast text should render inertly. | Verified: no `innerHTML` or inline `onclick` remains in `showToast`; frontend build passed with pre-existing warnings only. | Critical | Proven | Fixed | Changed files: `web/admin-spa/src/utils/tools.js`, `audit/BUG_MAP.md`. |
| BUG-002 | Fixed | `/admin-next` now receives an admin-SPA CSP from `securityMiddleware`, and the early `/admin-next/` shortcut calls the same header helper before `sendFile`. | Verification command: `npm test -- securityHeaders.test.js --runInBand`; lint command: `npm run lint:check`. | Admin SPA responses should include a CSP without script `unsafe-eval`. | Verified: focused tests passed for both normal middleware and early-response paths; backend lint passed. | Critical | Proven | Fixed | Changed files: `src/middleware/auth.js`, `src/app.js`, `tests/securityHeaders.test.js`, `audit/BUG_MAP.md`. |
| BUG-003 | Fixed | `requestLogger` no longer logs parsed request bodies and no longer wraps `res.json` to persist JSON response bodies. | Verification command: `npm test -- requestLoggerRedaction.test.js --runInBand`; lint command: `npm run lint:check`. | Logs should exclude credentials and bearer/session tokens. | Verified: focused test passed with a login-like password and token; backend lint passed. | Critical | Proven | Fixed | Changed files: `src/middleware/auth.js`, `tests/requestLoggerRedaction.test.js`, `audit/BUG_MAP.md`. |
| BUG-004 | Fixed | Setup, startup reload, and password-change paths now use hash-only init storage through `src/utils/adminCredentials.js`; old plaintext files are rewritten without `adminPassword` on load. | Verification command: `npm test -- adminCredentials.test.js --runInBand`; lint command: `npm run lint:check`. | Persistent files should not contain the active admin password. | Verified: tests prove setup data, plaintext migration, and password updates write no plaintext password; backend lint passed. | Critical | Proven | Fixed | Changed files: `src/utils/adminCredentials.js`, `scripts/setup.js`, `src/app.js`, `src/routes/web.js`, `tests/adminCredentials.test.js`, `audit/BUG_MAP.md`. |
| BUG-005 | Fixed | `redeemCard` now acquires `quota_card:redeem_lock:<cardId>` before re-reading card state and applying side effects at `src/services/quotaCardService.js:46-65` and `src/services/quotaCardService.js:318-430`. Focused regression coverage lives in `tests/quotaCardConcurrency.test.js`. | Run `npx jest tests/quotaCardConcurrency.test.js --runInBand`. | Only one request should redeem a card; the second concurrent request should fail without extra credit or redemption records. | Verified: two concurrent same-card redemptions produced one fulfillment, one `已使用` rejection, one quota addition, and one redemption record. | Critical | Proven | Fixed | Fix priority complete. Changed files: `src/services/quotaCardService.js`, `tests/quotaCardConcurrency.test.js`, `audit/BUG_MAP.md`. Verification: focused Jest, full Jest, touched-file ESLint, and `npm run lint:check` passed. |
| BUG-006 | Fixed | `addTotalCostLimit` now uses a Redis Lua update at `src/services/apiKeyService.js:2771-2838`; `extendExpiry` now serializes latest-state calculations through `apikey:mutation_lock:<keyId>` at `src/services/apiKeyService.js:2738-2761` and `src/services/apiKeyService.js:2887-2963`. Quota-card redemption passes cap options at `src/services/quotaCardService.js:376-415`. | Run `npx jest tests/quotaCardConcurrency.test.js --runInBand`. | Concurrent credits/extensions should preserve all additions; `+5` and `+7` should end at `12`, and two one-day extensions should add two days. | Verified: focused tests ended quota at `12` and expiry at `2030-01-03T00:00:00.000Z`. | High | Proven | Fixed | Fix priority complete. Changed files: `src/services/apiKeyService.js`, `src/services/quotaCardService.js`, `tests/quotaCardConcurrency.test.js`, `audit/BUG_MAP.md`. Verification: focused Jest, full Jest, touched-file ESLint, and `npm run lint:check` passed. |
| BUG-007 | Fixed | Reserved fixed user routes now call `next('route')` before the admin `/:userId` middleware chain, allowing `/redemption-history` and `/quota-info` to reach their user handlers. | Verification command: `npm test -- userRoutesOrder.test.js --runInBand`; lint command: `npm run lint:check`. | Request should reach the user redemption or quota handler. | Verified: both route-order tests passed and `requireAdmin` was not called for fixed user routes. | High | Proven | Fixed | Changed files: `src/routes/userRoutes.js`, `tests/userRoutesOrder.test.js`, `audit/BUG_MAP.md`. |
| BUG-008 | Fixed | `checkAuth()` now returns a verification boolean after calling `/web/auth/user`, and the protected-route guard awaits it before redirecting. | Start the admin SPA dev server, set `localStorage.authToken`, mock `/webapi/web/auth/user` as successful, then load `/admin/dashboard`; build with `npm run build -- --outDir /tmp/mighty-admin-spa-bug008-dist --emptyOutDir`. | A valid stored token should preserve the protected route or await verification. | Verified: Playwright ended at `http://127.0.0.1:5177/admin/dashboard` with the token still present; admin SPA build passed with pre-existing warnings only. | High | Proven | Fixed | Changed files: `web/admin-spa/src/stores/auth.js`, `web/admin-spa/src/router/index.js`. |
| BUG-009 | Fixed | Admin SPA logout now calls `POST /web/auth/logout` through `logoutApi` before clearing local auth state; the header awaits that store action. | Verification command: `rg -n "auth/logout|logoutApi|await authStore\\.logout|authStore\\.logout\\(" web/admin-spa/src/utils/http_apis.js web/admin-spa/src/stores/auth.js web/admin-spa/src/components/layout/AppHeader.vue`; build command: `cd web/admin-spa && npm run build -- --outDir /tmp/mighty-admin-spa-bug009-dist --emptyOutDir`. | Logout should invalidate the backend session. | Verified: source path calls backend logout and frontend build passed with pre-existing warnings. | High | Proven | Fixed | Changed files: `web/admin-spa/src/utils/http_apis.js`, `web/admin-spa/src/stores/auth.js`, `web/admin-spa/src/components/layout/AppHeader.vue`, `audit/BUG_MAP.md`. |
| BUG-010 | Fixed | `/metrics` now requires `authenticateAdmin` before collecting Redis system stats. | Verification command: `rg -n "app\\.get\\('/metrics', authenticateAdmin" src/app.js`; lint command: `npm run lint:check`. | Metrics should be protected or cheap enough for unauthenticated use. | Verified: route source includes admin auth and backend lint passed. | High | Proven | Fixed | Changed files: `src/app.js`, `audit/BUG_MAP.md`. |
| BUG-011 | Fixed | `src/app.js` no longer imports or mounts bare `cors()`; all requests use `corsMiddleware`. `tests/corsPolicy.test.js` covers arbitrary-origin rejection and configured local-origin allowance. | Verification command: `npm test -- corsPolicy.test.js --runInBand`; lint command: `npm run lint:check`; source search: `rg -n "app\\.use\\(cors\\(\\)|require\\('cors'\\)|from 'cors'" src/app.js src`. | Production CORS should be explicit and fail closed. | Verified: arbitrary origins do not receive `Access-Control-Allow-Origin`, allowed local origin still does, and no bare CORS mount remains. | Medium | Proven | Fixed | Changed files: `src/app.js`, `tests/corsPolicy.test.js`, `audit/BUG_MAP.md`. |
| BUG-012 | Fixed | `parsePaginationQuery()` now requires integer `limit` between 1 and 100 and integer `offset >= 0`; both `/users/redemption-history` and `/apiStats/api/redemption-history` call it before `quotaCardService.getRedemptions`. | Verification command: `npm test -- userRoutesOrder.test.js apiStatsRedemptionPagination.test.js --runInBand`; lint command: `npm run lint:check`. | Invalid pagination should be rejected or clamped. | Verified: invalid user/public pagination returns `400` before history lookup; valid public pagination is passed as numeric `limit`/`offset`. | Low | Proven | Fixed | Changed files: `src/utils/pagination.js`, `src/routes/userRoutes.js`, `src/routes/apiStats.js`, `tests/userRoutesOrder.test.js`, `tests/apiStatsRedemptionPagination.test.js`, `audit/BUG_MAP.md`. |
| BUG-013 | Fixed | Runtime baseline is now Node 24 (`package.json` engines, all Docker stages, CI workflows, installer script, and docs), AWS SDK packages are updated to `^3.1057.0`, root and frontend audit fixes were applied, frontend Vite tooling is on a clean line, and npm/pnpm lockfiles were refreshed. | Verification commands: root `npm audit --json`; root `npm audit --omit=dev --json`; frontend `npm audit --json`; frontend `npm audit --omit=dev --json`; `corepack pnpm install --lockfile-only`; `DOCKER_BUILDKIT=1 docker build -t mighty-audit-node24 .`; final test/build commands in `audit/VERIFICATION_REPORT.md`. | Production dependency audit should not contain known critical/high vulnerabilities. | Verified: root full/production audit and frontend full/production audit all report zero vulnerabilities; Docker build passed. | Critical | Proven | Fixed | Changed files: `Dockerfile`, `package.json`, `package-lock.json`, `pnpm-lock.yaml`, `web/admin-spa/package.json`, `web/admin-spa/package-lock.json`, `README.md`, `README_EN.md`, `scripts/manage.sh`, `.github/workflows/pr-lint-check.yml`, `.github/workflows/auto-release-pipeline.yml`, `audit/BUG_MAP.md`. |
| BUG-014 | Fixed | Component auto-registration now excludes `src/components/user/CreateApiKeyModal.vue`, which is already explicitly imported by `UserApiKeysManager.vue`, so the duplicate auto-registration warning no longer appears. | Run `cd web/admin-spa && npm run build -- --outDir /tmp/mighty-admin-spa-bug01415-dist --emptyOutDir`. | Component auto-registration should not have ambiguous names. | Verified: build passed and did not emit the duplicate `CreateApiKeyModal` warning. | Low | Proven | Fixed | Changed files: `web/admin-spa/vite.config.js`. |
| BUG-015 | Fixed | Build maintenance updated caniuse-lite to `1.0.30001793`; full Element Plus plugin installation was removed; vendor chunks are split more granularly; XLSX export loads the 425 kB static asset only when export is invoked. | Run `cd web/admin-spa && npm run build -- --outDir /tmp/mighty-admin-spa-bug01415-dist --emptyOutDir`. | Admin SPA should avoid avoidable large initial chunks and stale browser targeting data. | Verified: build passed with no stale Browserslist warning and no chunks-over-500-kB warning; largest generated JS chunk was below 500 kB. | Low | Proven | Fixed | Changed files: `web/admin-spa/vite.config.js`, `web/admin-spa/src/main.js`, `web/admin-spa/src/views/ApiKeysView.vue`, `web/admin-spa/package-lock.json`. |
| BUG-016 | Fixed | Focused tests now cover confirmed-risk paths including `securityHeaders.test.js`, `requestLoggerRedaction.test.js`, `apiKeyQueryAuth.test.js`, `adminCredentials.test.js`, `userRoutesOrder.test.js`, `adminLoginRateLimit.test.js`, `configSecrets.test.js`, `featureFlags.test.js`, `corsPolicy.test.js`, `accountBalanceService.test.js`, `adminApiKeysPayloadRulesRoute.test.js`, `apiStatsRedemptionPagination.test.js`, `toastEscapingStatic.test.js`, `quotaCardConcurrency.test.js`, and `webhookOutboundPolicy.test.js`. | Verification commands: `npx jest tests/quotaCardConcurrency.test.js tests/webhookOutboundPolicy.test.js --runInBand`; `npm test -- --runInBand`; `npm run lint:check`. | Risky security and data-integrity paths should have focused tests. | Verified: focused quota/webhook suite passed with 8 tests; full backend suite passed with 32 suites, 281 passing tests, and 8 skipped. | Medium | Proven | Fixed | Confirmed quota-card concurrency and webhook outbound filtering now have focused regression coverage. |
| BUG-017 | Fixed | `package.json` `start` and `nodemon.json` now run `node src/app.js` directly instead of invoking mutating dev-only lint. | In a temp directory, copy `package.json` and `package-lock.json`, add a dummy `src/app.js`, run `npm ci --omit=dev --ignore-scripts`, then run `npm start`. | Runtime start should not require dev-only lint tools or mutate source. | Verified: disposable production install ran `npm start` and printed `dummy app started`; backend lint passed separately with `npm run lint:check`. | High | Proven | Fixed | Changed files: `package.json`, `nodemon.json`. |
| BUG-018 | Duplicate | Same defect as BUG-009. Flow row maps the admin logout user flow to the backend session invalidation gap. | Use BUG-009 reproduction. | Same as BUG-009. | Same as BUG-009. | High | Proven | Covered by BUG-009 | Keep for flow traceability only. |
| BUG-019 | Duplicate | Same core defect as BUG-003. Flow row maps account-management secret logging through the global request/response logger. | Use BUG-003 reproduction with account creation/update instead of login. | Credential-bearing account requests should be redacted. | Logger path can persist submitted/returned secrets. | Critical | Proven | Covered by BUG-003 | Keep for flow traceability only. |
| BUG-020 | Fixed | `/users/api-keys` now serializes count/create/count-update with a per-user Redis lock, preserving the configured max under concurrency. | Run `npx jest tests/userApiKeyCreationLock.test.js --runInBand`; full backend suite: `npm test -- --runInBand`. | Max-key limit and count should remain correct under concurrency. | Verified: concurrent create requests with max `1` returned one `201` and one `400`; only one key was created and the user key count was updated to `1`. | High | Proven | Fixed | Changed files: `src/routes/userRoutes.js`, `tests/userApiKeyCreationLock.test.js`. |
| BUG-021 | Fixed | API-key extraction no longer reads `req.query.key`, and `requestLogger` no longer writes raw query strings in messages or metadata. | Verification command: `npm test -- apiKeyQueryAuth.test.js requestLoggerRedaction.test.js --runInBand`; lint command: `npm run lint:check`. | Bearer API keys should be accepted only from headers and should not appear in logs. | Verified: `?key=` requests return missing API key without validation, and log output excludes raw query values. | Critical | Proven | Fixed | Changed files: `src/middleware/auth.js`, `tests/apiKeyQueryAuth.test.js`, `tests/requestLoggerRedaction.test.js`, `audit/BUG_MAP.md`. |
| BUG-022 | Fixed | Public API stats now stores only `lastApiId`, removes legacy `lastApiKey`, and strips `apiKey` from the URL without loading it into state. | Verification command: `rg -n "lastApiKey\|loadApiKeyFromStorage\|saveApiKeyToStorage\|route\\.query\\.apiKey\|urlApiKey\|localStorage\\.setItem\\('lastApiKey'" web/admin-spa/src/stores/apistats.js web/admin-spa/src/views/ApiStatsView.vue`; build command: `cd web/admin-spa && npm run build -- --outDir /tmp/mighty-admin-spa-bug022-dist --emptyOutDir`. | Public stats page should not persist or URL-carry bearer credentials. | Verified: no full-key persistence/loading remains; the only `apiKey` query handling strips the parameter; frontend build passed with pre-existing warnings. | Critical | Proven | Fixed | Changed files: `web/admin-spa/src/stores/apistats.js`, `web/admin-spa/src/views/ApiStatsView.vue`, `audit/BUG_MAP.md`. |
| BUG-023 | Fixed | Disabled-user redirects now use `getUserLoginUrl()` in `web/admin-spa/src/stores/user.js:7-18`, which derives the login URL from `APP_CONFIG.basePath`; the interceptor uses that URL at `web/admin-spa/src/stores/user.js:216-218`. | Verification command: `rg -n "window\\.location\\.href = '/user-login'|pathname !== '/user-login'|getUserLoginUrl|USER_LOGIN_PATH" web/admin-spa/src/stores/user.js web/admin-spa/src`; build command: `cd web/admin-spa && npm run build -- --outDir /tmp/mighty-admin-spa-bug023-dist --emptyOutDir`. | User should be redirected to `/admin-next/user-login` or the configured SPA login route. | Verified: hardcoded root redirect is gone and frontend build passed with pre-existing warnings. | Medium | Proven | Fixed | Changed files: `web/admin-spa/src/stores/user.js`, `audit/BUG_MAP.md`. |
| BUG-024 | Duplicate | Same defect family as BUG-005 and BUG-006. Flow row maps the quota-card user flow to non-atomic claim and non-atomic quota mutation. | Use BUG-005 and BUG-006 reproductions. | Same as BUG-005 and BUG-006. | Same as BUG-005 and BUG-006. | High | Proven | Covered by BUG-005 and BUG-006 | Keep for flow traceability only. |
| BUG-025 | Fixed | Refresh route returns balance at `src/routes/admin/accountBalance.js:52-71`; provider/script failures return `status: error`; `_buildResponse` in `src/services/account/accountBalanceService.js:703-740` now sets top-level `success` from the response status instead of always `true`. | Verification command: `npm test -- accountBalanceService.test.js --runInBand`; lint command: `npm run lint:check`. | A failed balance query should be counted and shown as failure. | Verified: provider failure returns `success: false` with `data.status === 'error'`, so the UI path that counts `!!response?.success` no longer counts it as success. | Medium | Proven | Fixed | Changed files: `src/services/account/accountBalanceService.js`, `tests/accountBalanceService.test.js`, `audit/BUG_MAP.md`. |
| BUG-026 | Fixed | Production config now rejects missing, too-short, or known placeholder `JWT_SECRET` and `ENCRYPTION_KEY` values before account-secret encryption can use them. | Verification command: `npm test -- configSecrets.test.js --runInBand`; lint command: `npm run lint:check`. | App should refuse weak/missing encryption secrets before storing provider credentials. | Verified: production placeholder secrets throw at config load, and strong production secrets load successfully. | Critical | Proven | Fixed | Changed files: tracked `config/config.example.js`, local ignored `config/config.js` mirror in this workspace, `tests/configSecrets.test.js`, `audit/BUG_MAP.md`. |
| BUG-027 | Fixed | Admin login now initializes Redis-backed IP and username-scoped limiters in `src/routes/web.js:13-82` and consumes both before credential validation in `src/routes/web.js:85-153`. Focused coverage exists in `tests/adminLoginRateLimit.test.js`. | Verification command: `npm test -- adminLoginRateLimit.test.js --runInBand`; lint command: `npm run lint:check`. | Repeated failed admin login attempts should receive backoff or `429`. | Verified: exceeded attempts return `429` with `Retry-After`, and credential lookup is not called after the limiter blocks. | High | Proven | Fixed | Changed files: `src/routes/web.js`, `tests/adminLoginRateLimit.test.js`, `audit/BUG_MAP.md`. |
| BUG-028 | Fixed | `config/config.example.js:237-242` and the local ignored `config/config.js` mirror now set `accountBalance.enableBalanceScript` only when `BALANCE_SCRIPT_ENABLED === 'true'`; `tests/featureFlags.test.js` covers the default-disabled path and explicit enable path. | Verification command: `npm test -- featureFlags.test.js accountBalanceService.test.js --runInBand`; lint command: `npm run lint:check`. | Server-side scripting feature should be disabled unless explicitly enabled. | Verified: absent `BALANCE_SCRIPT_ENABLED` disables script execution, while `BALANCE_SCRIPT_ENABLED=true` enables it. | High | Proven | Fixed | Changed files: tracked `config/config.example.js`, local ignored `config/config.js` mirror in this workspace, `tests/featureFlags.test.js`, `audit/BUG_MAP.md`. |
| BUG-029 | Fixed | Shared policy in `src/utils/outboundNetworkPolicy.js` blocks private/special IP ranges after DNS resolution and provides a pinned lookup. `sendHttpRequest` validates URLs, pins DNS, and disables redirects at `src/services/webhookService.js:334-348`; SMTP host validation is at `src/services/webhookService.js:287-304`; Telegram proxy validation is at `src/services/webhookService.js:468-497`. Focused coverage lives in `tests/webhookOutboundPolicy.test.js`. | Run `npx jest tests/webhookOutboundPolicy.test.js --runInBand`. | Webhook egress should block internal and metadata destinations before making outbound requests. | Verified: loopback HTTP URLs, private DNS answers, SMTP loopback hosts, and Telegram loopback proxy URLs are rejected before axios/nodemailer send. | High | Proven | Fixed | Fix priority complete. Changed files: `src/utils/outboundNetworkPolicy.js`, `src/services/webhookService.js`, `tests/webhookOutboundPolicy.test.js`, `audit/BUG_MAP.md`. Verification: focused Jest, full Jest, touched-file ESLint, and `npm run lint:check` passed. |
| BUG-030 | Fixed | Shared `validateApiKeyCostLimits()` now rejects invalid `rateLimitCost`, `dailyCostLimit`, `totalCostLimit`, and `weeklyOpusCostLimit` values for create and batch-create before `generateApiKey`; batch-create also applies token/concurrency/rate-window/rate-request integer validations. | Verification command: `npm test -- adminApiKeysPayloadRulesRoute.test.js --runInBand`; lint command: `npm run lint:check`. | Invalid limit values should be rejected consistently. | Verified: invalid single-create daily/rate cost values and batch-create weekly Opus cost values return `400` and do not call `generateApiKey`. | Medium | Proven | Fixed | Changed files: `src/routes/admin/apiKeys.js`, `tests/adminApiKeysPayloadRulesRoute.test.js`, `audit/BUG_MAP.md`. |
| BUG-031 | Fixed | Cost-limited API keys now acquire a per-key cost-limit lock, revalidate the cost snapshot under the lock, estimate rated request cost, and reject projected limit overruns before relay. | Run `npx jest tests/costLimitConcurrencyLock.test.js --runInBand`; full backend suite: `npm test -- --runInBand`. | Concurrent requests should not collectively exceed configured cost limits. | Verified: two concurrent requests against daily limit `1` produced one `200` and one `402`, and final recorded daily cost stayed `0.75`. | High | Proven | Fixed | Changed files: `src/middleware/auth.js`, `tests/costLimitConcurrencyLock.test.js`. |

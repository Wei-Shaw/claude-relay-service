# Claude Relay Service 请求处理与错误处理完整手册

> **版本**: 2.0.0
> **更新日期**: 2025-12-07
> **新增功能**: 上游 5xx 自动故障转移机制 + noFailover 调试功能

---

## 📖 目录

1. [流程概览](#流程概览)
2. [Phase 0: 请求进入](#phase-0-请求进入-pre-auth)
3. [Phase 1: 路由匹配](#phase-1-路由匹配)
4. [Phase 2: 中间件验证](#phase-2-中间件链处理-authjs)
5. [Phase 3: 请求验证](#phase-3-请求体验证和权限检查-apijs)
6. [Phase 4: 账户调度](#phase-4-统一调度器-unifiedclaudeschedulerjs)
7. [Phase 5: 请求转发](#phase-5-请求转发-clauderelayservicejs)
8. [Phase 6: 响应处理](#phase-6-成功响应处理)
9. [Failover 机制详解](#failover-机制详解-新增)
10. [noFailover 调试功能](#nofailover-调试功能-新增)
11. [错误码汇总](#完整错误码汇总)
12. [特殊路由处理](#特殊路由处理)
13. [Redis Key 结构](#redis-key-结构参考)

---

## 流程概览

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                            Claude Relay Service 请求处理流程                      │
├─────────────────────────────────────────────────────────────────────────────────┤
│  Phase 0: 预处理 → Phase 1: 路由匹配 → Phase 2: 中间件验证 →                      │
│  Phase 3: 请求验证 → Phase 4: 账户调度 → Phase 5: 请求转发 →                      │
│  Phase 6: 响应处理 → Failover 机制 (可选)                                         │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## Phase 0: 请求进入 (Pre-Auth)

```
客户端请求
    │
    ▼
┌─────────────────────────────────────────┐
│  0️⃣ CORS 预检处理                        │
│  ├─ OPTIONS 请求 → 返回 CORS headers      │
│  └─ 其他请求 → 继续                       │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│  0️⃣ 请求体大小检查                        │
│  ├─ Content-Length > 60MB               │
│  │   ❌ 413 Request Entity Too Large     │
│  │   "Request body too large, limit: 10MB" │
│  └─ ≤60MB → 继续                         │
└─────────────────────────────────────────┘
    │
    ▼
  Phase 1
```

---

## Phase 1: 路由匹配

```
┌─────────────────────────────────────────────────────────────────┐
│                         路由分发                                 │
├─────────────────────────────────────────────────────────────────┤
│  /api/v1/messages          → Claude 官方 API 路由               │
│  /claude/v1/messages       → Claude 官方 API 路由 (别名)         │
│  /v1/messages/count_tokens → Token 计数路由 ⚠️ (跳过部分限制)    │
│  /gemini/v1/...            → Gemini API 路由                    │
│  /openai/v1/...            → OpenAI 兼容路由                    │
│  /openai/responses         → OpenAI Responses (Codex) 路由      │
│  /droid/...                → Droid (Factory.ai) 路由            │
│  /azure/...                → Azure OpenAI 路由                  │
│  /admin/...                → 管理后台路由                        │
│  /health                   → 健康检查 (无需认证)                  │
│  /metrics                  → 系统指标 (无需认证)                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## Phase 2: 中间件链处理 (auth.js)

### 标准路由处理流程

```
请求到达 authenticateApiKey 中间件
    │
    ▼
┌─────────────────────────────────────────┐
│  1️⃣ extractApiKey()                     │
│  从多个位置提取 API Key:                  │
│  ├─ x-api-key header                    │
│  ├─ x-goog-api-key header (Gemini CLI)  │
│  ├─ Authorization: Bearer <key>         │
│  ├─ api-key header                      │
│  └─ ?key= query parameter               │
│                                         │
│  ❌ 缺失: 401 Unauthorized               │
│     "Please provide an API key..."      │
└─────────────────────────────────────────┘
    │ ✅ 提取成功
    ▼
┌─────────────────────────────────────────┐
│  2️⃣ 基础格式验证                         │
│  ├─ 长度检查: 10-512 字符                 │
│                                         │
│  ❌ 格式无效: 401 Unauthorized            │
│     "API key format is invalid"         │
└─────────────────────────────────────────┘
    │ ✅ 格式正确
    ▼
┌─────────────────────────────────────────┐
│  3️⃣ apiKeyService.validateApiKey()      │
│  ├─ SHA-256 哈希查询 Redis               │
│  ├─ 检查 Key 是否存在                     │
│  ├─ 检查 Key 是否被禁用                   │
│  ├─ 检查 Key 是否过期                     │
│                                         │
│  ❌ 无效: 401 Unauthorized               │
│     "Invalid API key" + 具体原因         │
└─────────────────────────────────────────┘
    │ ✅ Key 有效
    ▼
┌─────────────────────────────────────────┐
│  4️⃣ 客户端限制检查 (ClientValidator)     │
│  ⚠️ count_tokens 路由跳过此检查           │
│                                         │
│  检查条件:                               │
│  ├─ enableClientRestriction = true      │
│  └─ allowedClients 数组配置              │
│                                         │
│  验证 User-Agent 是否匹配允许的客户端:     │
│  ├─ ClaudeCode                          │
│  ├─ Gemini-CLI                          │
│  ├─ Codex-CLI                           │
│  └─ 自定义客户端                         │
│                                         │
│  ❌ 不匹配: 403 Forbidden                │
│     "Your client is not authorized..."  │
│     Response: { allowedClients, userAgent } │
└─────────────────────────────────────────┘
    │ ✅ 客户端允许
    ▼
┌─────────────────────────────────────────┐
│  5️⃣ 并发限制检查                         │
│  ⚠️ count_tokens 路由跳过此检查           │
│                                         │
│  检查条件: concurrencyLimit > 0          │
│  实现: Redis Sorted Set                  │
│  Key: concurrency:{apiKeyId}            │
│                                         │
│  机制:                                   │
│  ├─ 获取当前并发数                        │
│  ├─ 检查是否超限                          │
│  ├─ 注册请求 (requestId + timestamp)     │
│  └─ 设置清理回调 (res.close/finish)       │
│                                         │
│  ❌ 超限: 429 Too Many Requests          │
│     "Too many concurrent requests"      │
│     Response: { currentConcurrency,     │
│                 concurrencyLimit }      │
└─────────────────────────────────────────┘
    │ ✅ 并发允许
    ▼
┌─────────────────────────────────────────┐
│  6️⃣ 速率限制检查 (时间窗口内)             │
│  ⚠️ count_tokens 路由跳过此检查           │
│                                         │
│  窗口配置: rateLimitWindow (分钟)         │
│                                         │
│  6a. 请求计数限制 (rateLimitRequests)    │
│      ❌ 429 "已达到请求次数限制 (N次)"     │
│                                         │
│  6b. Token 限制 (tokenLimit) - 旧格式    │
│      ❌ 429 "已达到 Token 使用限制"       │
│                                         │
│  6c. 费用限制 (rateLimitCost) - 新格式   │
│      ❌ 429 "已达到费用限制 ($X)"         │
│                                         │
│  Response: { resetAt, currentUsage }    │
└─────────────────────────────────────────┘
    │ ✅ 未触发限制
    ▼
┌─────────────────────────────────────────┐
│  7️⃣ 每日费用限制检查                      │
│  ⚠️ count_tokens 路由跳过此检查           │
│                                         │
│  检查条件: dailyCostLimit > 0            │
│  比较: dailyCost vs dailyCostLimit      │
│                                         │
│  ❌ 超限: 429 Too Many Requests          │
│     "已达到每日费用限制 ($X)"             │
│     Response: { resetAt: 明天0点 }       │
└─────────────────────────────────────────┘
    │ ✅ 每日限额未超
    ▼
┌─────────────────────────────────────────┐
│  8️⃣ 总费用限制检查                        │
│  ⚠️ count_tokens 路由跳过此检查           │
│                                         │
│  检查条件: totalCostLimit > 0            │
│  比较: totalCost vs totalCostLimit      │
│                                         │
│  ❌ 超限: 429 Too Many Requests          │
│     "已达到总费用限制 ($X)"               │
└─────────────────────────────────────────┘
    │ ✅ 总限额未超
    ▼
┌─────────────────────────────────────────┐
│  9️⃣ Opus 周费用限制检查                   │
│  ⚠️ count_tokens 路由跳过此检查           │
│                                         │
│  检查条件:                               │
│  ├─ weeklyOpusCostLimit > 0             │
│  └─ 请求模型包含 "claude-opus"           │
│                                         │
│  周期: 每周一 00:00 重置                  │
│                                         │
│  ❌ 超限: 429 Too Many Requests          │
│     "已达到 Opus 模型周费用限制 ($X)"     │
│     Response: { resetAt: 下周一 }        │
└─────────────────────────────────────────┘
    │ ✅ 所有验证通过
    ▼
┌─────────────────────────────────────────┐
│  ✅ 认证成功                              │
│                                         │
│  存储到请求对象:                          │
│  ├─ req.apiKey = { id, name, limits... }│
│  ├─ req.rateLimitInfo = { ... }         │
│  └─ req.concurrencyInfo = { ... }       │
│                                         │
│  调用 next() → Phase 3                  │
└─────────────────────────────────────────┘
```

---

## Phase 3: 请求体验证和权限检查 (api.js)

```
handleMessagesRequest()
    │
    ▼
┌─────────────────────────────────────────┐
│  1️⃣ 服务权限检查                         │
│                                         │
│  检查 req.apiKey.permissions:            │
│  ├─ 'all'    → 允许所有服务              │
│  ├─ 'claude' → 仅 Claude 服务            │
│  ├─ 'gemini' → 仅 Gemini 服务            │
│  └─ 'openai' → 仅 OpenAI 服务            │
│                                         │
│  ❌ 无权限: 403 Forbidden                │
│     "此 API Key 无权访问 Claude 服务"     │
└─────────────────────────────────────────┘
    │ ✅ 有权限
    ▼
┌─────────────────────────────────────────┐
│  2️⃣ 请求体验证                           │
│                                         │
│  验证项:                                 │
│  ├─ req.body 必须是对象                   │
│  ├─ req.body.messages 必须是数组          │
│  └─ messages 不能为空                     │
│                                         │
│  ❌ 无效: 400 Bad Request                │
│     具体的验证错误消息                     │
└─────────────────────────────────────────┘
    │ ✅ 请求体有效
    ▼
┌─────────────────────────────────────────┐
│  3️⃣ 模型黑名单检查                        │
│                                         │
│  检查条件:                               │
│  ├─ enableModelRestriction = true       │
│  └─ restrictedModels 数组有值            │
│                                         │
│  流程:                                   │
│  ├─ 从 body.model 获取模型名              │
│  ├─ getEffectiveModel() 去除供应商前缀    │
│  └─ 检查是否在 restrictedModels 中        │
│                                         │
│  ❌ 受限: 403 Forbidden                  │
│     "暂无该模型访问权限"                   │
└─────────────────────────────────────────┘
    │ ✅ 模型允许
    ▼
┌─────────────────────────────────────────┐
│  4️⃣ Claude Code 客户端检测               │
│  (仅 Claude 服务)                        │
│                                         │
│  检测 User-Agent 是否为 Claude Code:      │
│  ├─ 是 → 注入特殊 headers                │
│  │   ├─ claude-code-20250219            │
│  │   └─ System Prompt 注入               │
│  └─ 否 → 使用标准处理                     │
└─────────────────────────────────────────┘
    │
    ▼
  Phase 4: 账户调度
```

---

## Phase 4: 统一调度器 (unifiedClaudeScheduler.js)

```
selectAccountForApiKey(apiKeyData, sessionHash, requestedModel)
    │
    ▼
┌─────────────────────────────────────────┐
│  1️⃣ 供应商前缀解析                        │
│                                         │
│  parseVendorPrefixedModel(requestedModel)│
│  ├─ ccr:model-name → CCR 账户池          │
│  ├─ vendor:model-name → 特定供应商        │
│  └─ model-name → 默认账户池               │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│  2️⃣ CCR/特定供应商处理                    │
│                                         │
│  如果检测到 ccr: 前缀:                    │
│  └─ 调用 _selectCcrAccount()             │
│     ├─ 从 CCR 账户池选择                  │
│     └─ 检查模型支持性                     │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│  3️⃣ 专属账户/分组绑定检查                  │
│                                         │
│  检查 apiKeyData.claudeAccountId:        │
│                                         │
│  3a. 分组绑定 (group:xxx)                │
│      └─ selectAccountFromGroup()        │
│                                         │
│  3b. 单一账户绑定 (专属账户)              │
│      检查项:                             │
│      ├─ isActive === 'true'             │
│      ├─ status !== 'error'              │
│      ├─ isAccountTemporarilyUnavailable()│
│      └─ isAccountRateLimited()          │
│                                         │
│  ❌ 专属账户限流: 403 Forbidden           │
│     错误码: CLAUDE_DEDICATED_RATE_LIMITED │
│     "此专属账号已触发限流控制"             │
│     Response: { rateLimitEndAt }         │
└─────────────────────────────────────────┘
    │ ✅ 有可用专属账户 或 使用通用池
    ▼
┌─────────────────────────────────────────┐
│  4️⃣ 粘性会话检查                          │
│                                         │
│  Redis Key: unified_claude_session_     │
│             mapping:{sessionHash}       │
│                                         │
│  ├─ 存在且账户可用 → 使用绑定账户          │
│  ├─ 存在但账户不可用 → 清除绑定           │
│  └─ 不存在 → 进入通用池选择               │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│  5️⃣ 通用账户池过滤                        │
│                                         │
│  获取所有账户类型:                        │
│  ├─ claude-official (OAuth)             │
│  ├─ claude-console                      │
│  ├─ bedrock                             │
│  └─ ccr                                 │
│                                         │
│  过滤条件:                               │
│  ├─ isActive === 'true'                 │
│  ├─ status !== 'error'/'unauthorized'/  │
│  │          'blocked'                   │
│  ├─ schedulable === true                │
│  ├─ 未被临时标记不可用                    │
│  ├─ 未被过载标记                          │
│  └─ 支持请求的模型                        │
│                                         │
│  ⚠️ Console 账户并发预检查                │
│     (批量检查 maxConcurrentTasks)        │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│  6️⃣ 模型支持性检查                        │
│                                         │
│  Claude Official:                        │
│  ├─ 只支持 Claude 模型                    │
│  └─ Opus 订阅级别限制:                    │
│      ├─ Free: 无 Opus                    │
│      ├─ Pro: 仅 Opus 4.5+                │
│      └─ Max: 所有 Opus                   │
│                                         │
│  Claude Console:                         │
│  └─ 检查 supportedModels 配置             │
│                                         │
│  CCR:                                    │
│  └─ 检查 supportedModels 配置             │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│  7️⃣ 排序与选择                            │
│                                         │
│  排序优先级:                              │
│  ├─ 1. 账户优先级 (priority)             │
│  ├─ 2. 最后使用时间 (lastUsedAt)          │
│  └─ 3. 负载均衡考虑                       │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│  8️⃣ 粘性会话记录                          │
│                                         │
│  如果选中新账户:                          │
│  ├─ 记录 sessionHash → accountId 映射    │
│  └─ TTL: STICKY_SESSION_TTL_HOURS (默认1h)│
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│  ❌ 无可用账户                            │
│                                         │
│  情况1: Console 并发满                   │
│  └─ 503 Service Unavailable             │
│     错误码: CONSOLE_ACCOUNT_CONCURRENCY_FULL │
│     ⚠️ 会触发 api.js 层的重试逻辑          │
│                                         │
│  情况2: 无支持该模型的账户                 │
│  └─ 500 Internal Server Error           │
│     (抛出 Error，未被捕获为 503)          │
│                                         │
│  ✅ 成功                                 │
│  └─ 返回 { accountId, accountType }      │
└─────────────────────────────────────────┘
```

---

## Phase 5: 请求转发 (claudeRelayService.js)

```
relayRequest() / relayStreamRequestWithUsageCapture()
    │
    ▼
┌─────────────────────────────────────────┐
│  1️⃣ 获取账户完整信息并解密                 │
│                                         │
│  claudeAccountService.getAccount(id)     │
│  ├─ 解密 OAuth token                     │
│  └─ 解密敏感配置                          │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│  2️⃣ Access Token 获取/刷新               │
│  ⚠️ OAuth 刷新在此阶段执行                 │
│                                         │
│  getValidAccessToken(accountId):          │
│  ├─ 检查 token 过期时间                   │
│  ├─ 提前 10 秒刷新                        │
│  └─ 调用 OAuth refresh endpoint          │
│                                         │
│  ❌ 刷新失败: 500 Internal Server Error   │
│     (Token refresh failed)               │
└─────────────────────────────────────────┘
    │ ✅ Token 有效
    ▼
┌─────────────────────────────────────────┐
│  3️⃣ 请求头构建                            │
│                                         │
│  filterForClaude() 清理请求头:            │
│  ├─ 移除 CDN/代理相关 headers             │
│  ├─ 移除客户端 API Key                    │
│  └─ 添加账户凭据                          │
│                                         │
│  添加的 Headers:                          │
│  ├─ Authorization: Bearer {accessToken}  │
│  ├─ anthropic-version: 2023-06-01       │
│  └─ anthropic-beta: oauth-2025-04-20,   │
│     claude-code-20250219 (非Haiku),      │
│     interleaved-thinking-2025-05-14,     │
│     fine-grained-tool-streaming-2025-05-14│
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│  4️⃣ 代理配置                              │
│                                         │
│  如果账户配置了代理:                       │
│  ├─ ProxyHelper.createProxyAgent()       │
│  └─ 支持 SOCKS5 / HTTP 代理              │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│  5️⃣ 发送上游请求                          │
│                                         │
│  Target: https://api.anthropic.com/     │
│          v1/messages                    │
│                                         │
│  配置:                                   │
│  ├─ Timeout: REQUEST_TIMEOUT (默认600s) │
│  ├─ socket.setNoDelay(true)             │
│  └─ AbortController 用于取消             │
└─────────────────────────────────────────┘
    │
    ▼
  Phase 6 或 错误处理
```

---

## Phase 6: 成功响应处理

### 非流式响应 (stream = false)

```
上游返回 200/201
    │
    ▼
┌─────────────────────────────────────────┐
│  1️⃣ Usage 数据捕获                       │
│                                         │
│  从响应体提取:                            │
│  ├─ input_tokens                        │
│  ├─ output_tokens                       │
│  ├─ cache_creation_input_tokens         │
│  └─ cache_read_input_tokens             │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│  2️⃣ 成本计算                             │
│                                         │
│  costCalculator.calculateCost():         │
│  ├─ 获取模型价格 (pricingService)        │
│  ├─ 计算 input/output 成本               │
│  └─ 计算缓存相关成本                      │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│  3️⃣ 使用统计记录 (异步)                   │
│                                         │
│  queueRateLimitUpdate():                 │
│  ├─ 更新请求计数                          │
│  ├─ 更新 Token 计数                       │
│  ├─ 更新费用计数                          │
│  ├─ 更新每日/总费用                       │
│  └─ 更新 Opus 周费用 (如适用)             │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│  4️⃣ 错误状态清除                          │
│                                         │
│  如果请求成功:                            │
│  ├─ clearUnauthorizedErrors(accountId)   │
│  ├─ clearInternalErrors(accountId)       │
│  ├─ removeAccountRateLimit() (如之前限流) │
│  └─ removeAccountOverload() (如之前过载)  │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│  5️⃣ 响应返回                             │
│                                         │
│  res.status(200).json({                  │
│    id: "msg_...",                        │
│    type: "message",                      │
│    role: "assistant",                    │
│    content: [...],                       │
│    model: "claude-3-5-sonnet-20241022",  │
│    stop_reason: "end_turn",              │
│    usage: { ... }                        │
│  })                                      │
└─────────────────────────────────────────┘
```

### 流式响应 (stream = true)

```
上游返回 200，开始 SSE 流
    │
    ▼
┌─────────────────────────────────────────┐
│  1️⃣ 设置流式响应头                        │
│                                         │
│  res.setHeader('Content-Type',           │
│                'text/event-stream')      │
│  res.setHeader('Cache-Control', 'no-cache') │
│  res.setHeader('X-Accel-Buffering', 'no')│
│  res.socket.setNoDelay(true)             │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│  2️⃣ SSE 事件逐块转发                      │
│                                         │
│  event: message_start                    │
│  data: {...}                            │
│                                         │
│  event: content_block_start              │
│  data: {...}                            │
│                                         │
│  event: content_block_delta (多次)       │
│  data: {"delta": {"text": "..."}}       │
│                                         │
│  event: message_delta (包含 usage)       │
│  data: {"usage": {...}}                 │
│                                         │
│  event: message_stop                     │
│  data: {...}                            │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│  3️⃣ Usage 数据捕获                       │
│                                         │
│  从 message_delta 事件解析:              │
│  ├─ input_tokens                        │
│  ├─ output_tokens                       │
│  ├─ cache_creation_input_tokens         │
│  └─ cache_read_input_tokens             │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│  4️⃣ 成本计算和统计 (异步)                 │
│                                         │
│  不阻塞响应:                              │
│  ├─ costCalculator.calculateCost()       │
│  ├─ updateRateLimitCounters()            │
│  └─ 更新账户使用统计                      │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│  5️⃣ 连接关闭                             │
│                                         │
│  正常关闭:                               │
│  └─ res.end()                           │
│                                         │
│  客户端主动关闭 (req.on('close')):        │
│  ├─ decrementConcurrency()              │
│  ├─ abortController.abort()             │
│  └─ 返回 499 (不发送给客户端)            │
└─────────────────────────────────────────┘
```

---

## Failover 机制详解 (新增)

### 架构概览

```
┌──────────────────────────────────────────────────────────────────┐
│                        Failover 三层架构                          │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Layer 1: 中间件层 (authenticateApiKey)                          │
│  ├─ 401: 客户端 API Key 无效                                     │
│  ├─ 403: 客户端权限不足                                          │
│  └─ 429: 客户端触发限流                                          │
│  处理: ❌ 直接返回，不进入调度                                     │
│                                                                  │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Layer 2: 调度层 (unifiedClaudeScheduler)                        │
│  ├─ 503: 所有账户并发满                                          │
│  └─ 500: 无支持该模型的账户                                       │
│  处理: ❌ 直接返回 (503 可能触发重试)                              │
│                                                                  │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Layer 3: 上游 API 层 (Relay Services)                           │
│  ├─ 401/402: 账户认证失败                                        │
│  ├─ 403: 账户权限不足                                            │
│  ├─ 429: 账户限流                                                │
│  ├─ 502/504: 网关错误                                            │
│  ├─ 529: Claude 过载                                             │
│  └─ 5xx: 服务器错误                                              │
│  处理: 🔄 触发 failover（默认）或 ❌ 直接返回（noFailover=true）   │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

### 工作流程

```
┌──────────────────────────────────────────────────────────────────┐
│  客户端请求                                                       │
└────────┬─────────────────────────────────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────┐
│  1️⃣ 调度器选择账户 A                    │
│     └─ 排除 excludeAccountIds          │
└────────┬───────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────┐
│  2️⃣ Relay Service 发送请求              │
│     ├─ 使用账户 A 的凭据                │
│     └─ 发送到上游 API                   │
└────────┬───────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────────────────────────────┐
│  3️⃣ 上游返回错误 (401/429/5xx)                                 │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  3a. 标记账户状态（影响下次调度）                                │
│      ├─ 401/402 → markUnauthorized (永久失效)                  │
│      ├─ 403 → markForbidden (永久失效)                         │
│      ├─ 429 → markRateLimited (临时失效, TTL)                  │
│      ├─ 502/504 → markTemporarilyUnavailable (临时失效)        │
│      └─ 529 → markOverloaded (临时失效, 配置分钟数)             │
│                                                                │
│  3b. 检查 account.noFailover                                   │
│      ├─ true → 直接返回错误给客户端 ❌                           │
│      └─ false → 抛出错误触发 failover 🔄                        │
│                                                                │
└────────┬───────────────────────────────────────────────────────┘
         │ (noFailover = false)
         ▼
┌────────────────────────────────────────┐
│  4️⃣ failoverWrapper 捕获错误             │
│     ├─ 检查 shouldRetry()               │
│     │   ├─ isEnabled?                  │
│     │   ├─ error.noRetry?              │
│     │   ├─ isStreamStarted?            │
│     │   └─ retryCount < maxRetries?    │
│     └─ 决定是否重试                      │
└────────┬───────────────────────────────┘
         │ ✅ 可以重试
         ▼
┌────────────────────────────────────────┐
│  5️⃣ 更新 failover 上下文                │
│     ├─ retryCount++                    │
│     └─ excludeAccountIds.push(A)       │
└────────┬───────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────┐
│  6️⃣ 调度器选择账户 B                    │
│     ├─ 排除账户 A                       │
│     └─ 选择下一个可用账户                │
└────────┬───────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────┐
│  7️⃣ 使用账户 B 重试请求                 │
│     ├─ 成功 → 返回响应 ✅                │
│     └─ 失败 → 重复步骤 3-7              │
│        (最多 maxRetries 次)             │
└────────┬───────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────┐
│  8️⃣ 所有重试都失败                      │
│     └─ 返回最后一次的错误 ❌             │
└────────────────────────────────────────┘
```

### 配置参数

| 参数 | 环境变量 | 默认值 | 说明 |
|------|---------|--------|------|
| **启用开关** | `FAILOVER_ENABLED` | `true` | 是否启用 failover |
| **最大重试次数** | `FAILOVER_MAX_RETRIES` | `3` | 最多切换 3 次账户（总共 4 次尝试） |
| **临时不可用 TTL** | `FAILOVER_TEMP_UNAVAILABLE_TTL` | `300` | 账户被标记临时不可用的时长（秒） |
| **可重试状态码** | `FAILOVER_RETRYABLE_STATUS_CODES` | `[401,402,403,429,500,502,503,504,529]` | 触发 failover 的状态码 |
| **可重试错误码** | `FAILOVER_RETRYABLE_ERROR_CODES` | `[ECONNRESET,ETIMEDOUT,...]` | 触发 failover 的网络错误码 |

### 关键设计决策

#### 1. 状态标记与 Failover 分离

**原则**: 状态标记**只影响下次调度**，不影响当前 failover

```javascript
// ✅ 正确的理解
if (response.status === 401) {
  await markUnauthorized(accountId)  // 影响下次调度
  throw error  // 触发当前 failover
}

// ❌ 错误的理解
if (response.status === 401) {
  await markUnauthorized(accountId)
  return error  // 不触发 failover（这是 noFailover=true 的行为）
}
```

#### 2. 所有上游错误都触发 Failover

**原因**: 每个账户是独立的上游服务
- 账户 A 的 401 ≠ 账户 B 的 401
- 账户 A 的 429 ≠ 账户 B 的 429
- 账户 A 的 5xx ≠ 账户 B 的 5xx

**例外**: `account.noFailover = true` 时直接返回（调试用）

#### 3. 流式响应保护

```javascript
if (res.headersSent) {
  // 流已开始，无法重试
  logger.warn('Stream already started, cannot retry')
  throw error  // 只标记账户，不触发 failover
}
```

---

## noFailover 调试功能 (新增)

### 功能概述

**noFailover** 是账户级别的调试开关，启用后该账户遇到错误时**直接返回给客户端**，不触发 failover 机制。

### 使用场景

1. **调试特定账户**: 查看某个账户的真实错误响应
2. **客户端需要真实错误**: 某些客户端需要看到上游 API 的原始错误
3. **测试错误处理**: 验证上游 API 的错误响应格式

### 数据流

```
用户在 Web 界面启用 noFailover
    ↓
存储到 Redis (字符串 'true')
    ↓
accountService 读取并转换为布尔值 true
    ↓
relay service 检查 account.noFailover === true
    ↓
遇到错误时:
  1. 仍然标记账户状态
  2. 直接返回错误给客户端
  3. ❌ 不抛出异常
  4. ❌ 不触发 failover
```

### 实现架构

#### 1. 账户服务层

**所有账户服务**（8个）都支持 noFailover 字段：

```javascript
// createAccount
async createAccount(options = {}) {
  const {
    noFailover = false,  // 默认 false
    // ...
  } = options

  const accountData = {
    noFailover: noFailover.toString(),  // 转为字符串存储
    // ...
  }
}

// getAccount
async getAccount(accountId) {
  // ...
  accountData.noFailover = accountData.noFailover === 'true' || accountData.noFailover === true
  return accountData
}

// updateAccount
async updateAccount(accountId, updates) {
  if (updates.noFailover !== undefined) {
    updates.noFailover = updates.noFailover.toString()
  }
  // ...
}
```

#### 2. 中继服务层

**所有 relay services**（8个）都实现 noFailover 检查：

```javascript
// 错误处理模式
if (response.status >= 400) {
  const errorData = response.data

  // 1. 标记账户状态（影响下次调度）
  if (response.status === 401) {
    await accountService.markUnauthorized(account.id, reason)
  } else if (response.status === 429) {
    await accountService.markRateLimited(account.id, ttl)
  } else if (response.status >= 500) {
    await accountService.markTemporarilyUnavailable(account.id, ttl)
  }

  // 2. 检查 noFailover
  if (account.noFailover === true) {
    logger.info(`Account ${account.name} has noFailover=true, returning error directly`)
    return res.status(response.status).json(errorData)
  }

  // 3. 抛出错误触发 failover（默认行为）
  const error = new Error(errorMessage)
  error.statusCode = response.status
  error.accountId = account.id
  error.errorData = errorData  // 保存完整错误响应
  throw error
}
```

#### 3. Failover 核心层

**failoverHelper.js** 支持 error.noRetry 标志：

```javascript
shouldRetry(context) {
  if (!this.isEnabled()) {
    return false
  }

  // 检查显式的 noRetry 标志
  if (context.error?.noRetry || context.error?.noFailover) {
    logger.debug('🔄 Failover: Error marked as noRetry, skipping failover')
    return false
  }

  // ... 其他检查
}
```

**failoverWrapper.js** 传递完整 error 对象：

```javascript
const retryContext = {
  retryCount: failoverContext.retryCount,
  statusCode,
  errorCode,
  isStreamStarted: res.headersSent,
  error  // 传递完整 error 对象
}
```

#### 4. Web 管理界面

**AccountForm.vue** (通用表单):

```vue
<div class="form-group">
  <label class="flex items-center space-x-2">
    <input
      type="checkbox"
      v-model="formData.noFailover"
      class="form-checkbox"
    />
    <span>不触发 Failover（调试用）</span>
  </label>
  <p class="help-text">
    启用后，该账户遇到错误时将直接返回给客户端，不会尝试其他账户
  </p>
</div>
```

**CcrAccountForm.vue** (CCR 专用):

```vue
<!-- 同上，独立实现 -->
```

### 行为对比

#### noFailover = false (默认)

```
请求 → 账户A返回401
     → 标记A为unauthorized
     → 抛出错误
     → failover捕获
     → 尝试账户B
     → 成功/继续重试（最多3次）
```

#### noFailover = true (调试)

```
请求 → 账户A返回401
     → 标记A为unauthorized（仍执行）
     → 检查noFailover=true
     → 记录日志
     → 直接返回401 + errorData
     → ❌ 不触发failover
```

### 日志示例

```log
2025-12-07 10:30:15 INFO Account MyTestAccount has noFailover=true, returning 401 error directly
```

### 注意事项

1. **状态标记仍执行**: 即使 noFailover=true，账户状态仍会被标记（影响下次调度）
2. **errorData 完整性**: 返回给客户端的错误包含完整的上游错误响应
3. **向后兼容**: 默认值为 false，不影响现有账户
4. **调试专用**: 生产环境建议关闭，仅用于调试

---

## 上游响应状态码处理

```
┌─────────────────────────────────────────────────────────────────┐
│  6️⃣ 上游响应状态码处理                                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────┬────────────────────────────────────────────────┐  │
│  │ 状态码  │ 处理逻辑                                        │  │
│  ├─────────┼────────────────────────────────────────────────┤  │
│  │ 200/201 │ ✅ 成功 → Phase 6                              │  │
│  ├─────────┼────────────────────────────────────────────────┤  │
│  │ 400     │ 检查是否 "Organization disabled"               │  │
│  │         │ ├─ 是 → 标记账户 blocked                       │  │
│  │         │ └─ 否 → 直接返回错误给客户端                    │  │
│  ├─────────┼────────────────────────────────────────────────┤  │
│  │ 401     │ 🔐 Unauthorized                                │  │
│  │         │ ├─ 记录 401 错误计数 (Redis)                   │  │
│  │         │ ├─ 计数 > 1 → 标记账户 unauthorized            │  │
│  │         │ ├─ noFailover=true → 返回 401 ❌                │  │
│  │         │ └─ noFailover=false → 🔄 触发 Failover          │  │
│  ├─────────┼────────────────────────────────────────────────┤  │
│  │ 403     │ 🚫 Forbidden                                   │  │
│  │         │ ├─ 标记账户 blocked                            │  │
│  │         │ ├─ noFailover=true → 返回 403 ❌                │  │
│  │         │ └─ noFailover=false → 🔄 触发 Failover          │  │
│  ├─────────┼────────────────────────────────────────────────┤  │
│  │ 429     │ 🚦 Rate Limited                                │  │
│  │         │ ├─ markAccountRateLimited()                    │  │
│  │         │ ├─ 提取 reset 时间                             │  │
│  │         │ ├─ 专属账户/Opus限制 → 返回 403                 │  │
│  │         │ ├─ noFailover=true → 返回 429 ❌                │  │
│  │         │ └─ noFailover=false → 🔄 触发 Failover          │  │
│  ├─────────┼────────────────────────────────────────────────┤  │
│  │ 502     │ 🔌 Bad Gateway                                 │  │
│  │         │ ├─ 记录服务器错误                               │  │
│  │         │ ├─ markAccountTemporarilyUnavailable()         │  │
│  │         │ ├─ noFailover=true → 返回 502 ❌                │  │
│  │         │ └─ noFailover=false → 🔄 触发 Failover          │  │
│  ├─────────┼────────────────────────────────────────────────┤  │
│  │ 504     │ ⏱️ Gateway Timeout                             │  │
│  │         │ ├─ 记录超时错误                                 │  │
│  │         │ ├─ markAccountTemporarilyUnavailable()         │  │
│  │         │ ├─ noFailover=true → 返回 504 ❌                │  │
│  │         │ └─ noFailover=false → 🔄 触发 Failover          │  │
│  ├─────────┼────────────────────────────────────────────────┤  │
│  │ 529     │ 🔥 Over Capacity (Claude 特有)                 │  │
│  │         │ ├─ markAccountOverloaded()                     │  │
│  │         │ │   Redis Key: overload:{accountId}            │  │
│  │         │ │   TTL: CLAUDE_OVERLOAD_HANDLING_MINUTES      │  │
│  │         │ ├─ noFailover=true → 返回 529 ❌                │  │
│  │         │ └─ noFailover=false → 🔄 触发 Failover          │  │
│  ├─────────┼────────────────────────────────────────────────┤  │
│  │ 5xx     │ 🔥 其他服务器错误                               │  │
│  │         │ ├─ 记录错误                                     │  │
│  │         │ ├─ noFailover=true → 返回 5xx ❌                │  │
│  │         │ └─ noFailover=false → 🔄 触发 Failover          │  │
│  └─────────┴────────────────────────────────────────────────┘  │
│                                                                 │
│  🔄 Failover 自动重试机制 (v1.1.0 新增):                        │
│     ├─ 启用条件: FAILOVER_ENABLED=true (默认启用)              │
│     ├─ 可重试状态码: 401/402/403/429/500/502/503/504/529      │
│     ├─ 最大重试次数: FAILOVER_MAX_RETRIES (默认3次)            │
│     ├─ 账户临时不可用: FAILOVER_TEMP_UNAVAILABLE_TTL (默认5分钟) │
│     ├─ 流式请求保护: 已发送响应头则仅标记，不重试               │
│     ├─ noFailover 账户: 直接返回错误，不触发 failover          │
│     └─ 专属账户回退: 专属账户失败后自动回退到共享池             │
│                                                                 │
│  ⚠️ 其他即时重试场景 (api.js 层):                               │
│     CONSOLE_ACCOUNT_CONCURRENCY_FULL → 清除会话 → 重新调度(1次) │
└─────────────────────────────────────────────────────────────────┘
```

---

## 完整错误码汇总

### 客户端错误 (4xx)

| 状态码 | 错误名称 | 触发条件 | 响应示例 |
|-------|---------|---------|---------|
| **400** | Bad Request | 请求体无效、缺失必需字段、JSON 格式错误 | `{error: "message required"}` |
| **401** | Unauthorized | API Key 缺失/无效/过期/被禁用、上游账户认证失败 | `{error: "Invalid API key"}` |
| **403** | Forbidden | 权限不足、客户端受限、模型受限、专属账户限流、账户被封 | `{error: "Permission denied"}` |
| **413** | Request Entity Too Large | 请求体超过 60MB | `{error: "Request body too large"}` |
| **429** | Too Many Requests | 并发/请求数/Token/费用限制、上游账户限流 | `{error, limit, resetAt}` |
| **499** | Client Closed Request | 客户端在响应完成前关闭连接 (流式) | 无响应 (仅记录) |

### 服务器错误 (5xx)

| 状态码 | 错误名称 | 触发条件 | 响应示例 |
|-------|---------|---------|---------|
| **500** | Internal Server Error | 中间件异常、Token刷新失败、无可用账户(未捕获) | `{error: "Internal error"}` |
| **501** | Not Implemented | count_tokens 对 CCR/Bedrock 不支持 | `{error: "Not supported"}` |
| **502** | Bad Gateway | 上游连接重置、DNS失败、连接拒绝 | `{error: "Bad gateway"}` |
| **503** | Service Unavailable | Console 账户并发满、所有账户不可用 | `{error: "No available accounts"}` |
| **504** | Gateway Timeout | 上游请求超时 | `{error: "Gateway timeout"}` |
| **529** | Over Capacity | Claude API 过载 (Claude 特有) | `{error: "Overloaded"}` |

---

## 特殊路由处理

### count_tokens 路由 (/v1/messages/count_tokens)

```
⚠️ 跳过以下检查 (skipKeyRestrictions = true):
├─ 客户端限制检查
├─ 并发限制检查
├─ 速率限制检查
├─ 每日/总费用限制检查
└─ Opus 周费用限制检查

特殊处理:
├─ CCR/Bedrock 账户 → 501 Not Implemented
├─ Console 账户不支持 → 返回 { input_tokens: 0 }
└─ 客户端关闭 → 499 (仅记录)
```

---

## Redis Key 结构参考

```
# API Key 相关
api_key:{keyId}                    → Key 详情 (加密)
api_key_hash:{sha256_hash}         → keyId 映射 (快速查询)
api_key_usage:{keyId}              → 使用统计
api_key_cost:{keyId}               → 成本统计

# 速率限制
rate_limit:window_start:{keyId}    → 窗口开始时间
rate_limit:requests:{keyId}        → 请求计数
rate_limit:tokens:{keyId}          → Token 计数
rate_limit:cost:{keyId}            → 费用计数

# 并发控制
concurrency:{keyId}                → Sorted Set {requestId: timestamp}

# 账户相关
claude_account:{accountId}         → 账户信息 (加密)
claude_account:{accountId}:401_errors → 401 错误计数
overload:{accountId}               → 过载标记 (TTL 自动过期)
rate_limit_state:{accountId}       → 限流状态
temp_unavailable:{accountId}       → 临时不可用标记 (Failover TTL)

# 粘性会话
unified_claude_session_mapping:{hash} → {accountId, accountType, timestamp}
```

---

## 附录：代码实现位置索引

### Failover 核心
- **failoverHelper.js**: 119-123 行（noRetry 检查）
- **failoverWrapper.js**: 73 行（error 对象传递）

### 账户服务（noFailover 字段）
- **claudeAccountService.js**: 88, 126, 166, 421, 660, 693 行
- **claudeConsoleAccountService.js**: 55-123, 240-269, 357-383 行
- **geminiAccountService.js**: createAccount/getAccount/updateAccount
- **openaiResponsesAccountService.js**: 50, 79, 126, 158-160 行
- **bedrockAccountService.js**: createAccount/getAccount/updateAccount
- **azureOpenaiAccountService.js**: 112-145, 198-234 行
- **droidAccountService.js**: createAccount/getAccount/updateAccount
- **ccrAccountService.js**: createAccount/getAccount/updateAccount

### 中继服务（noFailover 检查）
- **claudeRelayService.js**: 新增 `_parseErrorData` 辅助方法
- **claudeConsoleRelayService.js**: 36, 69, 365, 414, 450, 528, 557, 878, 1107, 1181, 1286 行
- **geminiRelayService.js**: 69-154, 278-448, 517-673 行
- **openaiResponsesRelayService.js**: 144-156, 288-293, 314-321 行
- **bedrockRelayService.js**: 55, 119, 210, 452 行
- **azureOpenaiRelayService.js**: 168, 246, 768 行 + routes: 184, 283, 381 行
- **droidRelayService.js**: 新增 `_buildNoFailoverResponse` 方法
- **ccrRelayService.js**: 流式/非流式错误处理

### Web 界面
- **AccountForm.vue**: 通用账户表单复选框
- **CcrAccountForm.vue**: CCR 账户专用复选框

---

**文档版本**: 2.0.0
**最后更新**: 2025-12-07
**维护者**: Claude Relay Service Team
